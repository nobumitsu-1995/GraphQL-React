{
  "name": "server",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js",
    "build": "npx tsgo -p .",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'npx tsgo -p . && node dist/index.js'"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@typescript/native-preview": "^7.0.0-dev.20250614.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
